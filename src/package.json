{
  "name": "phyloscope",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node --experimental-vm-modules --test tests/*.test.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
